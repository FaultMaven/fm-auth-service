name: Release (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "This workflow is disabled."
          echo "We do not publish enterprise Docker Hub images from this repository."
          echo "Use .github/workflows/ci.yml for GHCR builds gated on tests+smoke."
