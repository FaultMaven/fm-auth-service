name: CD - Deploy (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "This workflow is disabled."
          echo "We deploy the on-prem stack via GHCR + faultmaven-enterprise-infra from .github/workflows/ci.yml."
          echo "If AWS/ECS deployment is needed later, re-enable this workflow but ensure it gates image publishes on tests+smoke."
